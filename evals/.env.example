# GitLab Production
# Token: set GITLAB_TOKEN, or authenticate via: glab auth login --hostname gitlab.com
GITLAB_TOKEN=
GITLAB_HOST=gitlab.com

# GitLab Staging
GITLAB_TOKEN_STAGING=
GITLAB_HOST_STAGING=staging.gitlab.com

# Model provider
ANTHROPIC_API_KEY=
