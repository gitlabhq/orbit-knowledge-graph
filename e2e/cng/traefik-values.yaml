# Traefik ingress controller for local GitLab
#
# Only non-default overrides. Everything else uses chart defaults.

resources:
  requests:
    cpu: 25m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 128Mi

logs:
  general:
    level: WARN
