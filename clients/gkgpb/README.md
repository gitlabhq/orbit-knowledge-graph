# Frozen

This module is superseded by `clients/orbitpb` (same wire contract, Orbit-branded names). Existing `clients/gkgpb/vX.Y.Z` tags keep working; no new versions are generated here. Removal is tracked in knowledge-graph#1152 (chain T10).
