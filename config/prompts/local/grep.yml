name: grep
version: 1.5.0
short: Search local definition names, paths, and bodies
description: >-
  Search local definition names, paths, and bodies.


  Returns ranked `Definition:<id>` references and source for the top three.
  Use `--path` or `--kind` to narrow results, then pass a reference to context
  for its body and relationships.
