name: mcp_serve
version: 1.1.0
short: Serve the local graph to MCP-compatible AI agents
description: >-
  Serve the local graph to MCP-compatible AI agents


  Plug into editors that support MCP (Claude Code, Cursor, OpenCode, Codex) so
  the agent can call `run_sql`, `get_graph_schema`, and `index`.
