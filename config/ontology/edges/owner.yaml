# config/ontology/edges/owner.yaml

description: Ownership relationship between users and groups/projects

variants:
  - from_node: { type: User, id: id }
    to_node: { type: Group, id: id }
    description: "User owns group."

sources:
  - node: Group
    from: {type: User, id: owner_id}
    to: {type: Group, id: id}
