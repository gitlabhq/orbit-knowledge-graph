description: Merge request's head pipeline (pipeline running against the tip of the source branch)

table: gl_ci_edge
scope: same_namespace
edges:
  - from: {node: MergeRequest, key: head_pipeline_id}
    to:   {node: Pipeline, key: id}
    mutable: true
