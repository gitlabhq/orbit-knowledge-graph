description: Commits added to a merge request, extracted from system notes (ADR 013)

variants:
  - from_node: { type: MergeRequest, id: id }
    to_node: { type: Commit, id: id }
    description: "MergeRequest had a commit added."
