description: Entity belongs to a pipeline

table: gl_ci_edge
scope: same_namespace
edges:
  - from: {node: SecurityScan, key: pipeline_id}
    to:   {node: Pipeline, key: id}
  - from: {node: Job, key: pipeline_id}
    to:   {node: Pipeline, key: id}
