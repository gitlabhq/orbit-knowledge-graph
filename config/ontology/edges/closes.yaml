# config/ontology/edges/closes.yaml

description: Merge request closes a work item when merged

variants:
  - from_node: { type: MergeRequest, id: id }
    to_node: { type: WorkItem, id: id }
    description: "Merge request closes work item."

etl:
  - scope: namespaced
    source: siphon_merge_requests_closing_issues
    order_by: [traversal_path, issue_id, id]
    from:
      id: merge_request_id
      type: MergeRequest
      columns: [state_id]  # denorm: MR state
    to:
      id: issue_id
      type: WorkItem
      columns: [state_id, work_item_type_id]  # denorm: WorkItem state + type
