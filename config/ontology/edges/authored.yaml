description: Authorship relationship between users and entities

scope: prune_to_target
edges:
  - from: {node: User, key: id}
    to:   {node: Note, key: author_id}
  - from: {node: User, key: id}
    to:   {node: MergeRequest, key: author_id}
  - from: {node: User, key: id}
    to:   {node: Vulnerability, key: author_id}
  - from: {node: User, key: id}
    to:   {node: WorkItem, key: author_id}
