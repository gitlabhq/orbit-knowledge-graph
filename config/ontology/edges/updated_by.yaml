description: User who most recently updated an entity (non-content changes)

scope: prune_to_target
edges:
  - from: {node: User, key: id}
    to:   {node: MergeRequest, key: updated_by_id}
    mutable: true
