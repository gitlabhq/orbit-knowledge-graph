description: User who most recently edited an entity's content (title/description)

scope: prune_to_target
edges:
  - from: {node: User, key: id}
    to:   {node: MergeRequest, key: last_edited_by_id}
    mutable: true
