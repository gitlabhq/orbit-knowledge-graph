# config/ontology/edges/affected_by.yaml

description: An affected-package entry is affected by a security advisory

variants:
  - from_node: { type: PmAffectedPackage, id: id }
    to_node: { type: PmAdvisory, id: id }
    fk_column: pm_advisory_id
    description: "Affected package entry belongs to advisory."
