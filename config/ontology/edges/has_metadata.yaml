# config/ontology/edges/has_metadata.yaml

description: Job has runtime metadata sourced from siphon_p_ci_builds_metadata
table: gl_ci_edge

variants:
  - from_node: { type: Job, id: id }
    to_node: { type: JobMetadata, id: id }
    description: "Job has metadata describing its effective timeout, interruptibility, and expanded environment."
