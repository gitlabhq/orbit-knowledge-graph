description: Merge request's most recent diff snapshot (points to the latest MergeRequestDiff)

scope: same_namespace
edges:
  - from: {node: MergeRequest, key: latest_merge_request_diff_id}
    to:   {node: MergeRequestDiff, key: id}
    mutable: true
