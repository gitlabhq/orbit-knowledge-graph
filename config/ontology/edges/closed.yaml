description: User who closed a work item or merge request

scope: prune_to_target
edges:
  - from: {node: User, key: id}
    to:   {node: WorkItem, key: closed_by_id}
  - from: {node: User, key: id}
    to:   {node: MergeRequest, key: closed_by_id}
