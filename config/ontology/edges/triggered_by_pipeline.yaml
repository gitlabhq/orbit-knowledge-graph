description: Job was triggered by an upstream pipeline (child pipeline relationship from the job side)

table: gl_ci_edge
edges:
  - from: {node: Job, key: upstream_pipeline_id}
    to:   {node: Pipeline, key: id}
