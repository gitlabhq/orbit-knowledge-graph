# config/ontology/edges/confirmed_by.yaml

description: User who confirmed a vulnerability

variants:
  - from_node: { type: User, id: id }
    to_node: { type: Vulnerability, id: id }
    fk_column: confirmed_by_id
    description: "User confirmed vulnerability."
