# config/ontology/nodes/core/note.yaml

node_type: Note
domain: core
description: A comment or annotation on a GitLab object (issue, merge request, commit, etc.)
label: id
destination_table: gl_note
default_columns: [id, noteable_type, noteable_id, created_at]

style:
  size: 24
  color: "#64748B"

redaction:
  resource_type: note
  id_column: id
  ability: read_note

properties:
  id:
    type: int64
    source: id
    nullable: false
    description: "Global identifier for the note."

  note:
    type: string
    source: note
    nullable: false
    description: "The text content of the note (markdown)."

  noteable_type:
    type: enum
    source: noteable_type
    nullable: false
    description: "Type of object this note is attached to."
    enum_type: string
    values:
      0: Issue
      1: MergeRequest
      2: Commit
      3: Snippet
      4: "WikiPage::Meta"
      5: "DesignManagement::Design"
      6: "AlertManagement::Alert"
      7: Vulnerability
      8: AbuseReport
      9: "ComplianceManagement::Projects::ComplianceViolation"

  noteable_id:
    type: int64
    source: noteable_id
    nullable: true
    description: "ID of the object this note is attached to (null for commits)."

  line_code:
    type: string
    source: line_code
    nullable: true
    description: "Code identifying the line position for diff notes."

  created_at:
    type: timestamp
    source: created_at
    nullable: true
    description: "Timestamp when the note was created."

  updated_at:
    type: timestamp
    source: updated_at
    nullable: true
    description: "Timestamp when the note was last updated."

  discussion_id:
    type: string
    source: discussion_id
    nullable: true
    description: "ID grouping notes into a discussion thread."

  resolved_at:
    type: timestamp
    source: resolved_at
    nullable: true
    description: "Timestamp when the note/discussion was resolved."

  internal:
    type: boolean
    source: internal
    nullable: false
    description: "Whether this note is only visible to project members."

  confidential:
    type: boolean
    source: confidential
    nullable: true
    description: "Whether this note is confidential."

  commit_id:
    type: string
    source: commit_id
    nullable: true
    description: "SHA of the commit this note is on (for commit notes)."

  st_diff:
    type: string
    source: st_diff
    nullable: true
    description: "Serialized diff snippet captured at note creation, populated for diff notes."

  traversal_path:
    type: string
    source: traversal_path
    filterable: false
    nullable: false
    description: "Namespace traversal path for authentication and authorization."

etl:
  type: query
  scope: namespaced
  select: "id, note, noteable_type, noteable_id, line_code, created_at, updated_at, discussion_id, resolved_at, internal, confidential, commit_id, st_diff, author_id, traversal_path"
  from: siphon_notes
  where: "system = false"
  traversal_path_filter: "startsWith(traversal_path, {traversal_path:String})"
  order_by: [traversal_path, noteable_type, noteable_id, id]
  edges:
    author_id:
      to: User
      as: AUTHORED
      direction: incoming
    noteable_id:
      to_column: noteable_type
      as: HAS_NOTE
      direction: incoming
      # Rails stores the polymorphic base class in `noteable_type` via
      # `Note#noteable_type=` → `klass.base_class.to_s`. WorkItem subclasses of
      # Issue (Task, Incident, Ticket, Objective, KeyResult, TestCase,
      # Requirement) all collapse to `Issue`. Legacy Epic notes created via
      # `Groups::Epics::NotesController` still arrive as `Epic` until the
      # unified work-item model fully replaces the legacy record.
      type_mapping:
        Issue: WorkItem
        Epic: WorkItem

storage:
  primary_key: [traversal_path, id]
  settings:
    add_minmax_index_for_temporal_columns: "1"
    auto_statistics_types: "'minmax, uniq, countmin'"
  columns:
    - {name: id, type: "Int64", codec: ["Delta(8)", "LZ4"]}
    - {name: note, type: "String", default: "''", codec: ["ZSTD(3)"]}
    - {name: noteable_type, type: "LowCardinality(String)", default: "''", codec: ["LZ4"]}
    - {name: noteable_id, type: "Nullable(Int64)", codec: ["Delta(8)", "LZ4"]}
    - {name: line_code, type: "Nullable(String)", codec: ["ZSTD(1)"]}
    - {name: commit_id, type: "Nullable(String)", codec: ["ZSTD(1)"]}
    - {name: st_diff, type: "Nullable(String)", codec: ["ZSTD(3)"]}
    - {name: discussion_id, type: "Nullable(String)", codec: ["ZSTD(1)"]}
    - {name: resolved_at, type: "Nullable(DateTime64(6, 'UTC'))", codec: ["Delta(8)", "ZSTD(1)"]}
    - {name: internal, type: "Bool", default: "false"}
    - {name: confidential, type: "Nullable(Bool)"}
    - {name: created_at, type: "Nullable(DateTime64(6, 'UTC'))", codec: ["Delta(8)", "ZSTD(1)"]}
    - {name: updated_at, type: "Nullable(DateTime64(6, 'UTC'))", codec: ["Delta(8)", "ZSTD(1)"]}
    - {name: traversal_path, type: "String", default: "'0/'", codec: ["ZSTD(1)"]}
  indexes:
    - {name: idx_noteable_type, column: noteable_type, type: "set(14)", granularity: 2}
    - {name: idx_internal, column: internal, type: "minmax", granularity: 1}
    - {name: idx_confidential, column: confidential, type: "minmax", granularity: 1}
    - {name: idx_id, column: id, type: "bloom_filter(0.01)", granularity: 1}
    - {name: idx_note, column: note, type: "text(tokenizer = splitByNonAlpha)", granularity: 1}
    - {name: idx_note_ngram, column: note, type: "ngrambf_v1(3, 512, 2, 0)", granularity: 1}
    - {name: idx_discussion_id, column: discussion_id, type: "bloom_filter(0.01)", granularity: 1}
  projections:
    - {type: reorder, name: by_id, order_by: [id]}
    - {type: reorder, name: by_noteable, order_by: [traversal_path, noteable_type, noteable_id, id]}
    - {type: aggregate, name: tp_count, select: [traversal_path, "uniq(id)"], group_by: [traversal_path]}
