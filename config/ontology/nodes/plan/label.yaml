# config/ontology/nodes/plan/label.yaml

node_type: Label
domain: plan
description: A label for categorizing and filtering work items
label: title
destination_table: gl_label
default_columns: [id, title, color]

style:
  size: 22
  color: "#A855F7"

redaction:
  resource_type: label
  id_column: id
  ability: read_label

properties:
  id:
    type: int64
    source: id
    nullable: false
    description: "Global identifier for the label."

  title:
    type: string
    source: title
    nullable: false
    description: "Title/name of the label."

  description:
    type: string
    source: description
    nullable: false
    description: "Description of the label."

  color:
    type: string
    source: color
    nullable: false
    description: "Hex color code for the label."

  created_at:
    type: timestamp
    source: created_at
    nullable: true
    description: "Timestamp when the label was created."

  updated_at:
    type: timestamp
    source: updated_at
    nullable: true
    description: "Timestamp when the label was last updated."

  project_id:
    type: int64
    source: project_id
    nullable: true
    description: "Project this label belongs to, if project-scoped. Null for group labels. Stored on the node row so the compiler can filter by project without scanning the edge table."

  group_id:
    type: int64
    source: group_id
    nullable: true
    description: "Group this label belongs to, if group-scoped. Null for project labels."

  traversal_path:
    type: string
    source: traversal_path
    filterable: false
    nullable: false
    description: "Namespace traversal path for authentication and authorization."

etl:
  type: table
  scope: namespaced
  source: siphon_labels
  edges:
    project_id:
      to: Project
      as: IN_PROJECT
      direction: outgoing
    group_id:
      to: Group
      as: IN_GROUP
      direction: outgoing

storage:
  primary_key: [traversal_path, id]
  settings:
    allow_part_offset_column_in_projections: "1"
    auto_statistics_types: "'minmax, uniq, countmin'"
  columns:
    - {name: id, type: "Int64", codec: ["Delta(8)", "LZ4"]}
    - {name: title, type: "String", default: "''", codec: ["ZSTD(1)"]}
    - {name: description, type: "String", default: "''", codec: ["ZSTD(3)"]}
    - {name: color, type: "String", default: "''", codec: ["ZSTD(1)"]}
    - {name: created_at, type: "Nullable(DateTime64(6, 'UTC'))", codec: ["Delta(8)", "ZSTD(1)"]}
    - {name: updated_at, type: "Nullable(DateTime64(6, 'UTC'))", codec: ["Delta(8)", "ZSTD(1)"]}
    - {name: project_id, type: "Nullable(Int64)", codec: ["Delta(8)", "LZ4"]}
    - {name: group_id, type: "Nullable(Int64)", codec: ["Delta(8)", "LZ4"]}
    - {name: traversal_path, type: "String", default: "'0/'", codec: ["ZSTD(1)"]}
  indexes:
    - {name: idx_id, column: id, type: "bloom_filter(0.01)", granularity: 1}
    - {name: idx_title, column: title, type: "text(tokenizer = splitByNonAlpha)", granularity: 1}
    - {name: idx_title_ngram, column: title, type: "ngrambf_v1(3, 512, 2, 0)", granularity: 1}
    - {name: idx_description, column: description, type: "text(tokenizer = splitByNonAlpha)", granularity: 1}
    - {name: idx_description_ngram, column: description, type: "ngrambf_v1(3, 512, 2, 0)", granularity: 1}
  projections:
    - {type: reorder, name: by_id, order_by: [id]}
    - {type: lightweight, name: by_project_id, order_by: [project_id]}
    - {type: lightweight, name: by_group_id, order_by: [group_id]}
    - {type: aggregate, name: tp_count, select: [traversal_path, "uniq(id)"], group_by: [traversal_path]}
