# yaml-language-server: $schema=../../schemas/setup_agent.schema.json
# OpenCode has no PreToolUse-style context injection, so a tool.execute.before plugin prepends
# a one-per-session graph reminder to the first bash command.
name: opencode
title: OpenCode
detect: ["$XDG_CONFIG_HOME/opencode", "~/.config/opencode"]
instruction_file:
  project: AGENTS.md
  global: "~/.config/opencode/AGENTS.md"
template_files:
  - path:
      project: .opencode/plugins/orbit.js
      global: "~/.config/opencode/plugins/orbit.js"
    template: opencode_plugin.js
registrations:
  - file:
      project: .opencode/opencode.json
      global: "~/.config/opencode/opencode.json"
    path: [plugin]
    value:
      project: .opencode/plugins/orbit.js
      global: "~/.config/opencode/plugins/orbit.js"
mcp:
  file:
    project: opencode.json
    global: "~/.config/opencode/opencode.json"
  format: opencode
skills:
  dir:
    project: .agents/skills
    global: "~/.agents/skills"
