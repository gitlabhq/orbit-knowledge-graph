# yaml-language-server: $schema=../../schemas/setup_agent.schema.json
# Codex rejects additionalContext on PreToolUse hooks, so AGENTS.md is the
# whole integration.
name: codex
instruction_file:
  project: AGENTS.md
  global: "~/.codex/AGENTS.md"
