[package]
name = "orbit-search"
version.workspace = true
edition = "2024"

[lints]
workspace = true

[features]
duckdb = ["dep:duckdb-client", "dep:arrow", "dep:anyhow", "dep:rayon"]

[dependencies]
rust-stemmers = { workspace = true }
duckdb-client = { path = "../duckdb-client", optional = true }
arrow = { workspace = true, optional = true }
anyhow = { workspace = true, optional = true }
rayon = { workspace = true, optional = true }
