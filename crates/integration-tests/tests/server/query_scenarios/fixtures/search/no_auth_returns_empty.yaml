# source: crates/integration-tests/tests/server/data_correctness/search.rs::search_no_auth_returns_empty
description: No redaction authorization returns zero nodes

config:
  redaction: none

query:
  json: |
    {
      "query_type": "traversal",
      "nodes": [{"id": "u", "entity": "User", "id_range": {"start": 1, "end": 10000},
                 "columns": ["username"]}],
      "limit": 10
    }

expect:
  node_count: 0
