# source: crates/integration-tests/tests/server/data_correctness/security.rs::cross_org_aggregation_excludes_other_org
description: Org-1 aggregation counts members only in org-1 groups

config:
  security: { org_id: 1, paths: ["1/"] }

query:
  json: |
    {
      "query_type": "aggregation",
      "nodes": [
        {"id": "g", "entity": "Group", "id_range": {"start": 1, "end": 10000}, "columns": ["name"]},
        {"id": "u", "entity": "User"}
      ],
      "relationships": [{"type": "MEMBER_OF", "from": "u", "to": "g"}],
      "group_by": ["g"],
      "aggregations": [{"count": "u", "as": "member_count"}],
      "limit": 10
    }

expect:
  groups:
    g:
      rows:
        - { entity: Group, id: 100, values: { member_count: 3 } }
      absent:
        - { entity: Group, id: 900 }
