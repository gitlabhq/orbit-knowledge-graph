# source: crates/integration-tests/tests/server/data_correctness/security.rs::aggregation_user_disconnected_scoped_node_rejects_at_compile
description: Aggregation with User and declared-but-disconnected Group rejects because User scan would be unbounded

query:
  json: |
    {
      "query_type": "aggregation",
      "nodes": [
        {"id": "u", "entity": "User", "filters": {"email": "target@example.com"}},
        {"id": "g", "entity": "Group"}
      ],
      "group_by": ["g"],
      "aggregations": [{"count": "u", "as": "hit"}],
      "limit": 1
    }

expect:
  compile_error: "globally-scoped"
  compile_error_not_contains:
    - "target@example.com"
