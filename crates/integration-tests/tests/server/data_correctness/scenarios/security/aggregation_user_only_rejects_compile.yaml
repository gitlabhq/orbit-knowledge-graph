# source: crates/integration-tests/tests/server/data_correctness/security.rs::aggregation_user_only_rejects_at_compile
description: Aggregation on User alone rejects at compile for non-admin because User has no traversal_path

query:
  json: |
    {
      "query_type": "aggregation",
      "nodes": [{"id": "u", "entity": "User", "id_range": {"start": 1, "end": 10000}, "columns": ["username"]}],
      "aggregations": [{"count": "u", "as": "cnt"}],
      "limit": 10
    }
  gql: |
    MATCH (u:User) WHERE u.id >= 1 AND u.id <= 10000 RETURN count(u) AS cnt LIMIT 10

expect:
  compile_error: true
