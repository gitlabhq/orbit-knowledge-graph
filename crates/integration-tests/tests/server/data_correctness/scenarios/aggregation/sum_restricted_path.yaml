# source: crates/integration-tests/tests/server/data_correctness/aggregation.rs::aggregation_sum_with_restricted_path
description: Sum with path restricted to 1/100/ excludes groups 101 and 102

config:
  security:
    org_id: 1
    paths: ["1/100/"]

query:
  json: |
    {
      "query_type": "aggregation",
      "nodes": [
        {"id": "g", "entity": "Group", "id_range": {"start": 1, "end": 10000}, "columns": ["name"]},
        {"id": "u", "entity": "User"}
      ],
      "relationships": [{"type": "MEMBER_OF", "from": "u", "to": "g"}],
      "group_by": ["g"],
      "aggregations": [{"sum": "u.id", "as": "id_sum"}],
      "limit": 10
    }
  gql: |
    MATCH (g:Group)<-[:MEMBER_OF]-(u:User) WHERE g.id >= 1 AND g.id <= 10000 RETURN g{.id, .name}, sum(u.id) AS id_sum LIMIT 10

expect:
  groups:
    g:
      rows:
        - { entity: Group, id: 100, values: { id_sum: 9 } }
      absent:
        - { entity: Group, id: 101 }
        - { entity: Group, id: 102 }
