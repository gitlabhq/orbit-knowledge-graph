# source: crates/integration-tests/tests/server/data_correctness/search.rs::search_virtual_filter_without_node_ids_rejected
description: Virtual column filter without node_ids is rejected at compile time

query:
  json: |
    {
      "query_type": "traversal",
      "nodes": [{"id": "f", "entity": "File",
               "columns": ["path"],
               "filters": {
                   "project_id": {"eq": 1000},
                   "content": {"contains": "ClickHouse"}
               }}],
      "limit": 10
    }

expect:
  compile_error: "node_ids"
