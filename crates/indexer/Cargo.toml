[package]
name = "indexer"
version.workspace = true
edition = "2024"
license = "LicenseRef-EE"

[features]
default = []
testkit = ["nats-client/testkit"]
# Dev-only in-process memory harness for the ClickHouse indexing path.
mem-harness = ["dep:mimalloc"]

[[bin]]
name = "ch-mem-harness"
path = "src/bin/ch_mem_harness.rs"
required-features = ["mem-harness"]

[dependencies]
async-nats = { workspace = true }
async-trait = { workspace = true }
arrow = { workspace = true }
mimalloc = { workspace = true, optional = true }
base64 = { workspace = true }
bytes = { workspace = true }
chrono = { workspace = true }
clickhouse = { workspace = true }
clickhouse-client = { workspace = true }
code-graph = { path = "../code-graph" }
croner = { workspace = true }
orbit-analytics = { path = "../orbit-analytics" }
orbit-observability = { workspace = true }
orbit-server-config = { path = "../orbit-server-config" }
petgraph = { workspace = true }
orbit-utils = { path = "../utils" }
datafusion = { workspace = true }
flate2 = { workspace = true }
futures = { workspace = true }
gitaly-protos = { path = "../gitaly-protos" }
gitlab-client = { workspace = true }
indexmap = { workspace = true }
labkit-events = { workspace = true }
moka = { workspace = true }
nats-client = { workspace = true }
ontology = { path = "../ontology" }
query-engine = { path = "../query-engine" }
opentelemetry = { workspace = true }
parking_lot = { workspace = true }
prost = { workspace = true }
rand = { workspace = true }
regex = { workspace = true }
rustc-hash = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
siphon-proto = { workspace = true }
tar = { workspace = true }
tempfile = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true }
tokio-stream = { workspace = true }
tokio-util = { workspace = true }
tracing = { workspace = true }
uuid = { workspace = true }
axum = { workspace = true }
zstd = "0.13.3"

[dev-dependencies]
orbit-analytics = { path = "../orbit-analytics", features = ["testkit"] }
jsonschema = { workspace = true }
nats-client = { workspace = true, features = ["testkit"] }
tokio = { workspace = true, features = ["rt-multi-thread", "time", "test-util", "macros"] }
tokio-stream = { workspace = true }
tower = { workspace = true }

# This crate opts into the workspace lints AND additionally denies bare `#[allow(..)]`
# attributes. A crate-local `[lints.clippy]` table replaces (does not merge with) the
# workspace `[workspace.lints.clippy]` block, so the workspace lints are re-declared here.
# Rationale: handler/pipeline work in this crate previously shipped scaffold-era
# `#[allow(dead_code)]` markers (see !1416). Denying allows-without-reason turns every
# `#[allow(..)]` into a reviewable, justified `#[allow(.., reason = "..")]`. See
# `crates/indexer/AGENTS.md` and the no-shipped-dead-code rule in the root `AGENTS.md`.
[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = { level = "warn", priority = -1 }
allow_attributes_without_reason = "deny"
