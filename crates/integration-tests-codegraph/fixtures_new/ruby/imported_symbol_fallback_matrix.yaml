name: "Ruby: imported-symbol fallback matrix"
fixtures:
  - path: app/api_runner.rb
    content: |
      require "faraday"
      require "external_client"
      require "json"

      class ApiRunner
        def fetch
          Faraday.get("https://example.com")
        end

        def connect
          ExternalClient.connect
        end

        def parse
          JSON.parse("{}")
        end
      end

  - path: app/relative_runner.rb
    content: |
      require_relative "services/external_client"

      class RelativeRunner
        def connect
          ExternalClient.connect
        end
      end

  - path: app/local_external_client.rb
    content: |
      require "local_external_client"

      class LocalExternalClient
        def self.connect
        end
      end

      class ShadowRunner
        def connect
          LocalExternalClient.connect
        end
      end

  - path: app/local_runner.rb
    content: |
      class LocalRunner
        def run
          helper
        end

        def helper
        end
      end

tests:
  # corrected: a constant reference is an autoload import of the constant, not a require attribution
  - name: "gem requires target side-effect imported symbols"
    query: |
      MATCH (caller:Definition)-[e:CALLS]->(sym:ImportedSymbol)
      WHERE caller.fqn = 'ApiRunner::fetch'
      RETURN caller.fqn AS caller, sym.import_path AS path, sym.identifier_name AS name, sym.import_type AS import_type
    assert:
      - { row_count: 1 }
      - { row: { caller: "ApiRunner::fetch", path: "Faraday", name: "Faraday", import_type: "Autoload" } }

  # corrected: a constant reference is an autoload import of the constant, not a require attribution
  - name: "underscored requires target side-effect imported symbols"
    query: |
      MATCH (caller:Definition)-[e:CALLS]->(sym:ImportedSymbol)
      WHERE caller.fqn = 'ApiRunner::connect'
      RETURN caller.fqn AS caller, sym.import_path AS path, sym.identifier_name AS name, sym.import_type AS import_type
    assert:
      - { row_count: 1 }
      - { row: { caller: "ApiRunner::connect", path: "ExternalClient", name: "ExternalClient", import_type: "Autoload" } }

  # corrected: a constant reference is an autoload import of the constant, not a require attribution
  - name: "observed acronym constants match require paths without path-derived names"
    query: |
      MATCH (caller:Definition)-[e:CALLS]->(sym:ImportedSymbol)
      WHERE caller.fqn = 'ApiRunner::parse'
      RETURN caller.fqn AS caller, sym.import_path AS path, sym.identifier_name AS name, sym.import_type AS import_type
    assert:
      - { row_count: 1 }
      - { row: { caller: "ApiRunner::parse", path: "JSON", name: "JSON", import_type: "Autoload" } }

  # corrected: a constant reference is an autoload import of the constant, not a require attribution
  - name: "require_relative target imported symbols"
    query: |
      MATCH (caller:Definition)-[e:CALLS]->(sym:ImportedSymbol)
      WHERE caller.fqn = 'RelativeRunner::connect'
      RETURN caller.fqn AS caller, sym.import_path AS path, sym.identifier_name AS name, sym.import_type AS import_type
    assert:
      - { row_count: 1 }
      - { row: { caller: "RelativeRunner::connect", path: "ExternalClient", name: "ExternalClient", import_type: "Autoload" } }

  - name: "locally resolved require constants do not emit imported-symbol fallback"
    queries:
      - query: |
          MATCH (caller:Definition)-[e:CALLS]->(callee:Definition)
          WHERE caller.fqn = 'ShadowRunner::connect'
            AND callee.fqn = 'LocalExternalClient::connect'
          RETURN caller.fqn AS caller, callee.fqn AS callee
        assert:
          - { row_count: 1 }
      - query: |
          MATCH (caller:Definition)-[e:CALLS]->(sym:ImportedSymbol)
          WHERE caller.fqn = 'ShadowRunner::connect'
          RETURN sym.import_path AS path
        assert:
          - { empty: true }

  - name: "resolved same-class calls do not emit imported-symbol fallback"
    queries:
      - query: |
          MATCH (caller:Definition)-[e:CALLS]->(callee:Definition)
          WHERE caller.fqn = 'LocalRunner::run'
            AND callee.fqn = 'LocalRunner::helper'
          RETURN caller.fqn AS caller, callee.fqn AS callee
        assert:
          - { row_count: 1 }
      - query: |
          MATCH (caller:Definition)-[e:CALLS]->(sym:ImportedSymbol)
          WHERE caller.fqn = 'LocalRunner::run'
          RETURN sym.import_path AS path
        assert:
          - { empty: true }
