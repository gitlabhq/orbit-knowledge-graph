name: Java call resolution
fixtures:
  - path: com/example/Service.java
    content: |
      package com.example;

      public class Service {
          public void process() {}
          public void run() {
              process();
          }
      }
  - path: com/example/App.java
    content: |
      package com.example;

      public class App {
          public void main() {
              new Service();
          }
      }

tests:
  # ── Original targeted queries, hardened ──────────────────────

  - name: Same-class method call resolves
    query: |
      MATCH (caller:Definition)-[:DefinitionToDefinition]->(callee:Definition)
      WHERE caller.name = 'run' AND callee.name = 'process'
      RETURN caller.fqn AS caller, callee.fqn AS callee
    assert:
      - { row_count: 1 }
      - { row: { caller: "com.example.Service.run", callee: "com.example.Service.process" } }

  - name: Cross-file constructor call resolves
    query: |
      MATCH (caller:Definition)-[:DefinitionToDefinition]->(callee:Definition)
      WHERE caller.file_path <> callee.file_path
      RETURN caller.fqn AS caller, callee.fqn AS callee
    assert:
      - { row_count: 1 }
      - { row: { caller: "com.example.App.main", callee: "com.example.Service" } }

  - name: Package scoping produces correct FQNs
    query: |
      MATCH (d:Definition)
      WHERE d.name = 'Service'
      RETURN d.fqn AS fqn
    assert:
      - { row_count: 1 }
      - { row: { fqn: "com.example.Service" } }

  # ── Broader exhaustive checks ────────────────────────────────

  - name: Exactly 5 definitions with correct types and files
    query: |
      MATCH (d:Definition)
      RETURN d.name AS name, d.fqn AS fqn, d.definition_type AS type, d.file_path AS file
    assert:
      - { row_count: 5 }
      - { unique: fqn }
      - { no_nulls: fqn }
      - { no_nulls: type }
      - { column_values: { field: name, values: ["Service", "process", "run", "App", "main"] } }
      - { row: { name: "Service", fqn: "com.example.Service", type: "Class", file: "com/example/Service.java" } }
      - { row: { name: "process", fqn: "com.example.Service.process", type: "Method", file: "com/example/Service.java" } }
      - { row: { name: "run", fqn: "com.example.Service.run", type: "Method", file: "com/example/Service.java" } }
      - { row: { name: "App", fqn: "com.example.App", type: "Class", file: "com/example/App.java" } }
      - { row: { name: "main", fqn: "com.example.App.main", type: "Method", file: "com/example/App.java" } }

  - name: Exactly 3 containment edges
    query: |
      MATCH (parent:Definition)-[e:DefinitionToDefinition]->(child:Definition)
      WHERE e.edge_kind = 'Defines'
      RETURN parent.fqn AS parent, child.fqn AS child
    assert:
      - { row_count: 3 }
      - { row: { parent: "com.example.Service", child: "com.example.Service.process" } }
      - { row: { parent: "com.example.Service", child: "com.example.Service.run" } }
      - { row: { parent: "com.example.App", child: "com.example.App.main" } }

  - name: Exactly 2 files, both Java
    query: |
      MATCH (f:File)
      RETURN f.path AS path, f.language AS lang
    assert:
      - { row_count: 2 }
      - { match: { field: lang, pattern: "java" } }
      - { column_values: { field: path, values: ["com/example/Service.java", "com/example/App.java"] } }
