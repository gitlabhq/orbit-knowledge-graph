name: "Rust: module system (mod, pub use, re-exports)"
pipeline: rust
fixtures:
  - path: lib.rs
    content: |
      pub mod models;
      pub use models::User;

  - path: models.rs
    content: |
      pub struct User {
          pub name: String,
      }

      pub fn create_user(name: &str) -> User {
          User { name: name.into() }
      }

  - path: main.rs
    content: |
      use crate::models::create_user;

      fn run() {
          let u = create_user("alice");
      }

tests:
  - name: All definitions across modules
    query: |
      MATCH (d:Definition)
      WHERE d.definition_type IN ['Struct', 'Function']
      RETURN d.name AS name, d.file_path AS file
      ORDER BY name
    assert:
      - { row_count: 3 }
      - { row: { name: "User", file: "models.rs" } }
      - { row: { name: "create_user", file: "models.rs" } }
      - { row: { name: "run", file: "main.rs" } }

  - name: Import from crate::models extracted
    query: |
      MATCH (i:ImportedSymbol)
      WHERE i.file_path = 'main.rs'
      RETURN i.name AS name
    assert:
      - { row_count: 1 }
      - { row: { name: "create_user" } }

  - name: Cross-module call resolves
    query: |
      MATCH (caller:Definition)-[e:DefinitionToDefinition]->(callee:Definition)
      WHERE caller.name = 'run' AND callee.name = 'create_user'
        AND caller.file_path <> callee.file_path
        AND e.edge_kind = 'Calls'
      RETURN caller.fqn AS caller, callee.fqn AS callee
    assert:
      - { row_count: 1 }
