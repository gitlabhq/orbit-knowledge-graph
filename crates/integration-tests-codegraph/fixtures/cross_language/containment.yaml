name: Containment hierarchy
fixtures:
  - path: app.py
    content: |
      class Outer:
          class Inner:
              def method(self):
                  pass
          def outer_method(self):
              pass

tests:
  # ── Original targeted queries, hardened ──────────────────────

  - name: Nested class produces containment edge
    skip: true
    query: |
      MATCH (outer:Definition)-[:CALLS|DEFINES|EXTENDS]->(inner:Definition)
      WHERE outer.name = 'Outer' AND inner.name = 'Inner'
      RETURN outer.fqn AS parent, inner.fqn AS child
    assert:
      - { row_count: 1 }
      - { row: { parent: "app.Outer", child: "app.Outer.Inner" } }

  - name: Method belongs to its class
    query: |
      MATCH (cls:Definition)-[:CALLS|DEFINES|EXTENDS]->(m:Definition)
      WHERE cls.name = 'Outer' AND m.name = 'outer_method'
      RETURN cls.fqn AS parent, m.fqn AS child
    assert:
      - { row_count: 1 }
      - { row: { parent: "app.Outer", child: "app.Outer.outer_method" } }

  - name: FQN hierarchy is correct
    query: |
      MATCH (d:Definition)
      WHERE d.name = 'method'
      RETURN d.fqn AS fqn
    assert:
      - { row_count: 1 }
      - { row: { fqn: "app.Outer.Inner.method" } }

  # ── Broader exhaustive checks ────────────────────────────────

  - name: Exactly 4 definitions with correct types
    query: |
      MATCH (d:Definition)
      RETURN d.name AS name, d.fqn AS fqn, d.definition_type AS type, d.file_path AS file
    assert:
      - { row_count: 4 }
      - { unique: fqn }
      - { no_nulls: fqn }
      - { no_nulls: type }
      - { match: { field: file, pattern: "app.py" } }
      - { column_values: { field: name, values: ["Outer", "Inner", "method", "outer_method"] } }
      - { row: { name: "Outer", fqn: "app.Outer", type: "Class" } }
      - { row: { name: "Inner", fqn: "app.Outer.Inner", type: "Class" } }
      - { row: { name: "method", fqn: "app.Outer.Inner.method", type: "Method" } }
      - { row: { name: "outer_method", fqn: "app.Outer.outer_method", type: "Method" } }

  - name: Exactly 3 containment edges, all Defines
    query: |
      MATCH (parent:Definition)-[e:CALLS|DEFINES|EXTENDS]->(child:Definition)
      WHERE e.relationship_kind = 'Defines'
      RETURN parent.fqn AS parent, child.fqn AS child
    assert:
      - { row_count: 3 }
      - { row: { parent: "app.Outer", child: "app.Outer.Inner" } }
      - { row: { parent: "app.Outer", child: "app.Outer.outer_method" } }
      - { row: { parent: "app.Outer.Inner", child: "app.Outer.Inner.method" } }
      - { not: true, row: { parent: "app.Outer.Inner", child: "app.Outer.outer_method" } }

  - name: Single file with correct metadata
    query: |
      MATCH (f:File)
      RETURN f.path AS path, f.extension AS ext, f.language AS lang
    assert:
      - { row_count: 1 }
      - { row: { path: "app.py", ext: "py", lang: "python" } }

  - name: File owns all 4 definitions via Defines edges
    query: |
      MATCH (f:File)-[e:DEFINES|CALLS]->(d:Definition)
      WHERE e.relationship_kind = 'Defines'
      RETURN f.path AS file, d.name AS def
    assert:
      - { match: { field: file, pattern: "app.py" } }
      - { column_values: { field: def, values: ["Outer", "Inner", "method", "outer_method"] } }
