[package]
name = "query-engine"
version.workspace = true
edition = "2024"

[features]
default = []
testutils = ["formatters/testutils"]

[dependencies]
compiler = { path = "compiler" }
types = { path = "types" }
pipeline = { path = "pipeline" }
formatters = { path = "formatters" }
shared = { path = "shared" }

[lints]
workspace = true
