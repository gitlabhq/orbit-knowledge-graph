name: "FilterOnly — filtered node joined through edge"
input: {"query_type":"traversal","nodes":[{"id":"mr","entity":"MergeRequest","node_ids":[1]},{"id":"label","entity":"Label","filters":{"title":"bug"}}],"relationships":[{"type":"HAS_LABEL","from":"mr","to":"label"}],"limit":10}
expected:
  op: TopN
  input:
    op: Join
    left:
      op: Join
      left: {op: Scan, table: gl_edge, dedup: false}
      right: {op: Scan, table: gl_merge_request, dedup: true}
    right: {op: Scan, table: gl_label, dedup: true}
