name: "Two FK hops — all nodes joined, no edge tables"
input: {"query_type":"traversal","nodes":[{"id":"pipe","entity":"Pipeline","filters":{"status":"failed"}},{"id":"j","entity":"Job","filters":{"status":"failed"}},{"id":"p","entity":"Project","node_ids":[1]}],"relationships":[{"type":"HAS_JOB","from":"pipe","to":"j"},{"type":"IN_PROJECT","from":"pipe","to":"p"}],"limit":10}
expected: |
  (Limit 11
    (Project ["HAS_JOB":e0_type (pipe.id):e0_src "Pipeline":e0_src_type (j.id):e0_dst "Job":e0_dst_type "IN_PROJECT":e1_type (pipe.id):e1_src "Pipeline":e1_src_type (p.id):e1_dst "Project":e1_dst_type @pipe.id @pipe.iid @pipe.status @pipe.ref @pipe.source @j.id @j.name @j.status @j.ref @p.id @p.name @p.full_path @p.visibility_level]
      (Join Inner (pipe.project_id = p.id)
        (Join Inner (j.pipeline_id = pipe.id)
          (Filter [status:eq !deleted]
            (Scan gl_pipeline pipe FINAL))
          (Filter [status:eq !deleted]
            (Scan gl_job j FINAL)))
        (Filter [id∈[1] !deleted]
          (Scan gl_project p FINAL)))))
