name: "Neighbors outgoing — single edge scan, no FINAL"
input:
  json: |
    {"query_type":"neighbors","nodes":[{"id":"mr","entity":"MergeRequest","node_ids":[1]}],"neighbors":{"direction":"outgoing"},"limit":20}
expected:
  op: TopN
  input:
    op: Scan
    table: gl_edge
    dedup: false
