name: "Neighbors both direction"
input:
  json: |
    {
      "query_type": "neighbors",
      "nodes": [{"id": "mr", "entity": "MergeRequest", "node_ids": [1]}],
      "neighbors": {"direction": "both"},
      "limit": 20
    }
expected:
  op: TopN
  input:
    op: Union
    arms:
      - op: Scan
        table: gl_edge
      - op: Scan
        table: gl_edge
