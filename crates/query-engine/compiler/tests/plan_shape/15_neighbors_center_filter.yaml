name: "Neighbors with center filters joins node table with FINAL"
input:
  json: |
    {"query_type":"neighbors","nodes":[{"id":"mr","entity":"MergeRequest","filters":{"title":{"contains":"fix"}}}],"neighbors":{"direction":"outgoing"},"limit":20}
expected:
  op: TopN
  input:
    op: Join
    left:
      op: Scan
      table: gl_edge
      alias: e
      dedup: false
    right:
      op: Scan
      table: gl_merge_request
      alias: mr
      dedup: true
