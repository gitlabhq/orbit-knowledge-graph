name: "Single node scan uses FINAL"
input:
  json: |
    {"query_type":"traversal","nodes":[{"id":"mr","entity":"MergeRequest","filters":{"state":"merged"}}],"limit":10}
expected:
  op: TopN
  input:
    op: Scan
    table: gl_merge_request
    dedup: true
