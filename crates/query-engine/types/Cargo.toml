[package]
name = "types"
version.workspace = true
edition = "2024"
license = "MIT"

[dependencies]
arrow = { workspace = true }
gkg-utils = { path = "../../utils" }
compiler = { path = "../compiler" }

[dev-dependencies]
compiler = { path = "../compiler" }

[lints]
workspace = true
