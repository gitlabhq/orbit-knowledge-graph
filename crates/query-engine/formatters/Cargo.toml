[package]
name = "formatters"
version.workspace = true
edition = "2024"
license = "MIT"

[dependencies]
gkg-utils = { path = "../../utils" }
indexmap = { workspace = true }
shared = { path = "../shared" }
types = { path = "../types" }
compiler = { path = "../compiler" }
gkg-config = { path = "../../config" }
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
arrow = { workspace = true }

[lints]
workspace = true
