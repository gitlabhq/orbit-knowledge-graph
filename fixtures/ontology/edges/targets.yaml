# yaml-language-server: $schema=../ontology.schema.json
type: edge
description: Target branch relationships.
variants:
  - from_node: { type: MergeRequest, id: id }
    to_node: { type: Branch, id: name }
    meaning: "Merge request targets branch for merging."
    status: pending
    internal:
      sources:
        [
          { system: postgres, database: main, table: merge_requests, from_column: id, to_column: target_branch },
        ]
