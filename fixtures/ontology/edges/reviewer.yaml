# yaml-language-server: $schema=../ontology.schema.json
type: edge
description: Review assignment relationship between users and merge requests.
variants:
  - from_node: { type: User, id: id }
    to_node: { type: MergeRequest, id: id }
    meaning: "User is assigned as reviewer for merge request."
    status: pending
    internal:
      sources:
        [
          { system: postgres, database: main, table: merge_request_reviewers, from_column: user_id, to_column: merge_request_id },
          { system: clickhouse, table: null },
        ]
