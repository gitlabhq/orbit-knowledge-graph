# fixtures/ontology/edges/imports.yaml

description: Import relationships in source code

variants:
  - from_node: { type: File, id: id }
    to_node: { type: ImportedSymbol, id: id }
    description: "File contains an import statement."

  - from_node: { type: ImportedSymbol, id: id }
    to_node: { type: Definition, id: id }
    description: "Imported symbol resolves to a definition."

  - from_node: { type: ImportedSymbol, id: id }
    to_node: { type: ImportedSymbol, id: id }
    description: "Imported symbol references another imported symbol (re-exports)."

  - from_node: { type: ImportedSymbol, id: id }
    to_node: { type: File, id: id }
    description: "Imported symbol references a file (module import)."
