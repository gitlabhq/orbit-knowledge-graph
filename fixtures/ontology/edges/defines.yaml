# fixtures/ontology/edges/defines.yaml

description: Definition relationships in source code

variants:
  - from_node: { type: File, id: id }
    to_node: { type: Definition, id: id }
    description: "File defines a code definition (class, function, etc.)."

  - from_node: { type: Definition, id: id }
    to_node: { type: Definition, id: id }
    description: "Definition contains or calls another definition (inheritance, composition, method calls)."

  - from_node: { type: Definition, id: id }
    to_node: { type: ImportedSymbol, id: id }
    description: "Definition exports or is source of an imported symbol."
