# fixtures/ontology/edges/scans.yaml

description: Scanner scans a project for security vulnerabilities

variants:
  - from_node: { type: Scanner, id: id }
    to_node: { type: Project, id: id }
    description: "Scanner scans project."
