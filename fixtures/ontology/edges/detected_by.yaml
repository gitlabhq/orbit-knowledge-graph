# fixtures/ontology/edges/detected_by.yaml

description: Finding was detected by a scanner

variants:
  - from_node: { type: Finding, id: id }
    to_node: { type: Scanner, id: id }
    description: "Finding detected by scanner."
