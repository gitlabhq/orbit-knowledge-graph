# yaml-language-server: $schema=../ontology.schema.json
type: edge
description: User who confirmed an entity.
variants:
  - from_node: { type: User, id: id }
    to_node: { type: Vulnerability, id: id }
    meaning: "User confirmed vulnerability."
    status: pending
    internal:
      sources:
        [
          { system: postgres, database: main, table: vulnerabilities, from_column: confirmed_by_id, to_column: id },
          { system: clickhouse, table: null },
        ]
