# fixtures/ontology/nodes/plan/milestone.yaml

node_type: Milestone
domain: plan
description: A milestone for tracking progress toward a goal within a project or group
destination_table: gl_milestone

properties:
  id:
    type: int64
    source: id
    nullable: false
    description: "Global identifier for the milestone."

  iid:
    type: int64
    source: iid
    nullable: true
    description: "Project or group-scoped milestone number."

  title:
    type: string
    source: title
    nullable: false
    description: "Title of the milestone."

  description:
    type: string
    source: description
    nullable: true
    description: "Description of the milestone."

  state:
    type: enum
    source: state
    nullable: true
    description: "Current state of the milestone."
    values:
      0: active
      1: closed

  due_date:
    type: date
    source: due_date
    nullable: true
    description: "Due date for the milestone."

  start_date:
    type: date
    source: start_date
    nullable: true
    description: "Start date for the milestone."

  created_at:
    type: timestamp
    source: created_at
    nullable: true
    description: "Timestamp when the milestone was created."

  updated_at:
    type: timestamp
    source: updated_at
    nullable: true
    description: "Timestamp when the milestone was last updated."

# edges:
#   - type: IN_PROJECT
#     source_id: project_id
#     target_kind: Project
#
#   - type: IN_GROUP
#     source_id: group_id
#     target_kind: Group
