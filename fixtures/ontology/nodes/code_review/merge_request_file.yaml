# fixtures/ontology/nodes/code_review/merge_request_file.yaml

node_type: MergeRequestFile
domain: code_review
description: A file changed in a merge request diff
destination_table: gl_merge_request_file

properties:
  id:
    type: int64
    source: id
    nullable: false
    description: "TODO: Derived identifier for the merge request diff file (needs to be computed from merge_request_diff_id + relative_order or similar)."

  merge_request_diff_id:
    type: int64
    source: merge_request_diff_id
    nullable: false
    description: "ID of the merge request diff this file belongs to."

  too_large:
    type: boolean
    source: too_large
    nullable: false
    description: "Whether the diff is too large to display."

  new_path:
    type: string
    source: new_path
    nullable: true
    description: "Path to the file after the change."

  old_path:
    type: string
    source: old_path
    nullable: false
    description: "Path to the file before the change."

  new_file:
    type: boolean
    source: new_file
    nullable: false
    description: "Whether this is a newly added file."

  renamed_file:
    type: boolean
    source: renamed_file
    nullable: false
    description: "Whether this file was renamed."

  deleted_file:
    type: boolean
    source: deleted_file
    nullable: false
    description: "Whether this file was deleted."

  binary:
    type: boolean
    source: binary
    nullable: true
    description: "Whether this is a binary file."

# edges:
#   - type: IN_MERGE_REQUEST
#     source_id: merge_request_diff_id
#     target_kind: MergeRequest
