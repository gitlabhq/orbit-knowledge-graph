# fixtures/ontology/nodes/source_code/branch.yaml

node_type: Branch
domain: source_code
description: A Git branch in a repository
label: name
destination_table: gl_branch

style:
  size: 28
  color: "#14B8A6"

redaction:
  resource_type: project
  id_column: project_id
  ability: read_code

properties:
  id:
    type: int64
    source: id
    nullable: false
    description: "TODO: Derived identifier for the branch (needs to be computed from project_id + branch_name hash or similar)."

  project_id:
    type: int64
    source: project_id
    nullable: false
    description: "ID of the project this branch belongs to."

  name:
    type: string
    source: name
    nullable: false
    description: "Name of the branch (e.g., main, feature/new-login)."

  protected:
    type: boolean
    source: protected
    nullable: true
    description: "Whether the branch is protected."

  default:
    type: boolean
    source: default
    nullable: true
    description: "Whether this is the default branch of the project."

  traversal_path:
    type: string
    source: traversal_path
    nullable: false
    description: "Namespace traversal path for authentication and authorization."

# edges:
#   - type: IN_PROJECT
#     source_id: project_id
#     target_kind: Project

