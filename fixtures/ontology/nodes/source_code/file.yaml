# fixtures/ontology/nodes/source_code/file.yaml

node_type: File
domain: source_code
description: A source code file within a repository
label: path
destination_table: gl_file

style:
  size: 24
  color: "#22D3EE"

redaction: null  # TODO: configure redaction

properties:
  id:
    type: int64
    source: id
    nullable: false
    description: "Internal identifier for the file node."

  path:
    type: string
    source: path
    nullable: false
    description: "Relative path of the file within the repository."

  name:
    type: string
    source: name
    nullable: false
    description: "Name of the file including extension."

  language:
    type: string
    source: language
    nullable: true
    description: "Programming language of the file (e.g., ruby, python, java)."

  extension:
    type: string
    source: extension
    nullable: true
    description: "File extension (e.g., rb, py, java)."

  absolute_path:
    type: string
    source: absolute_path
    nullable: true
    description: "Absolute path of the file on the filesystem."

  repository_name:
    type: string
    source: repository_name
    nullable: true
    description: "Name of the repository this file belongs to."

  traversal_path:
    type: string
    source: traversal_path
    nullable: false
    description: "Namespace traversal path for authentication and authorization."

# edges:
#   - type: IN_DIRECTORY
#     source_id: directory_id
#     target_kind: Directory
#
#   - type: IN_PROJECT
#     source_id: project_id
#     target_kind: Project
