# fixtures/ontology/nodes/source_code/directory.yaml

node_type: Directory
domain: source_code
description: A directory within a repository's file structure
label: path
destination_table: gl_directory

style:
  size: 26
  color: "#06B6D4"

redaction:
  resource_type: projects
  id_column: project_id
  ability: read_code

properties:
  id:
    type: int64
    source: id
    nullable: false
    description: "Unique identifier for the directory node."

  traversal_path:
    type: string
    source: traversal_path
    nullable: false
    description: "Namespace traversal path for authentication and authorization."

  project_id:
    type: int64
    source: project_id
    nullable: false
    description: "ID of the project this directory belongs to."

  branch:
    type: string
    source: branch
    nullable: false
    description: "Git branch where this directory exists."

  path:
    type: string
    source: path
    nullable: false
    description: "Relative path of the directory within the repository."

  name:
    type: string
    source: name
    nullable: false
    description: "Name of the directory."
