# E2E GDK CI Test Infrastructure -- Progress

## Goal
Run the full GKG stack (GDK + Siphon + NATS + ClickHouse + GKG components)
in CI with HTTPS, test users, and Robot Framework e2e tests.

## Status: CONTAINER VERIFIED -- pushing image to registry

## What's Done
- [x] Docker image built and ALL services verified running
- [x] Robot Framework tests passing inside container (2/2 green)
  - Services Are Running: ClickHouse, NATS, GDK all verified
  - Seed Test Data: groups, projects, MRs, issues created via API
- [x] Full service stack operational:
  - Rails HTTPS at gdk.test:3443 (readiness OK)
  - ClickHouse at port 8123 (queries working)
  - NATS at port 4222 (connected)
  - Siphon CDC (producer + consumer active, LSN tracking)
  - nginx, Workhorse, Gitaly, PostgreSQL, Redis all up
- [x] Data seeding:
  - EE Ultimate license installed (expires +1 year)
  - Cloud Connector key created
  - knowledge_graph feature flag enabled
  - KG namespaces enabled for all root groups
  - Root password reset (prevents expiry-based 403)
  - PAT created and saved
- [x] CI job configured in .gitlab-ci.yml (manual trigger)
- [x] Robot Framework test suite (3 files, 10+ tests)
- [x] Branch pushed with 4 commits
- [ ] Image push to GitLab Container Registry (in progress)

## Fixes Applied
1. ClickHouse memory limit -> 8GB (default 3GB too low)
2. PostgreSQL wal_level -> logical (for Siphon CDC)
3. ClickHouse databases created + click_house.yml copied at startup
4. ClickHouse migrations run at startup
5. Root password reset (GDK default expires)
6. Namespace.root -> parent_id:nil (API compatibility)
7. Removed gitlab:duo:setup (not available)
8. GDK_GITLAB_DIR env var for portable paths
9. mise activation in shell commands for Rails runner
10. Simplified suite setup (verify API instead of re-enabling flags)

## What Remains
- [ ] Wait for image push to complete
- [ ] Validate CI job runs end-to-end
- [ ] Run the full test suite (including CDC + indexing tests)
- [ ] Update progress and finalize MR
