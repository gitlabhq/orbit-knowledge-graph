{
  "$schema": "http://iglucentral.com/schemas/com.snowplowanalytics.self-desc/schema/jsonschema/1-0-0#",
  "description": "Common context attached to every GKG (Orbit) event.",
  "self": {
    "vendor": "com.gitlab",
    "name": "orbit_common",
    "version": "1-0-0",
    "format": "jsonschema"
  },
  "type": "object",
  "additionalProperties": false,
  "required": ["deployment_type", "environment"],
  "properties": {
    "deployment_type": {
      "description": "Deployment type where the request originates from.",
      "type": "string",
      "enum": [".com", "dedicated", "self-managed"]
    },
    "environment": {
      "description": "Name of the source environment, such as `production` or `staging`",
      "type": "string",
      "maxLength": 32
    },
    "correlation_id": {
      "description": "Standard request trace ID. Enables end-to-end debugging of DAP to Orbit call chains.",
      "type": ["string", "null"],
      "maxLength": 64
    },
    "instance_id": {
      "type": ["string", "null"],
      "description": "ID of the GitLab instance where the request comes from",
      "maxLength": 255
    },
    "unique_instance_id": {
      "type": ["string", "null"],
      "description": "Unique ID of the GitLab instance where the request comes from",
      "maxLength": 255
    },
    "host_name": {
      "type": ["string", "null"],
      "description": "Host name of the GitLab instance where the request comes from",
      "maxLength": 255
    },
    "organization_id": {
      "description": "ID of the associated organization",
      "type": ["integer", "null"],
      "minimum": 0,
      "maximum": 2147483647
    },
    "root_namespace_ids": {
      "description": "Top-level group(s) of the namespace. Required for billing rollup and enterprise segmentation.",
      "type": ["array", "null"],
      "items": {
        "description": "Root namespace ID",
        "type": "integer",
        "minimum": 0,
        "maximum": 2147483647
      }
    },
    "schema_version": {
      "description": "Version of the schema contract used by the emitting service.",
      "type": ["string", "null"],
      "maxLength": 32
    }
  }
}
