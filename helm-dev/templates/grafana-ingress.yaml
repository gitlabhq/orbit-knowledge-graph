{{- if and .Values.observability.enabled .Values.observability.grafana.ingress.enabled .Values.observability.grafana.ingress.host }}
apiVersion: networking.gke.io/v1
kind: ManagedCertificate
metadata:
  name: grafana-cert
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "gkg.labels" . | nindent 4 }}
spec:
  domains:
    - {{ .Values.observability.grafana.ingress.host }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "gkg.labels" . | nindent 4 }}
  annotations:
    kubernetes.io/ingress.class: "gce"
    kubernetes.io/ingress.allow-http: "true"
    cloud.google.com/neg: '{"ingress": true}'
    networking.gke.io/managed-certificates: "grafana-cert"
spec:
  rules:
    - host: {{ .Values.observability.grafana.ingress.host }}
      http:
        paths:
          - path: /*
            pathType: ImplementationSpecific
            backend:
              service:
                name: {{ .Release.Name }}-grafana
                port:
                  number: 80
{{- end }}
