# Helm dependency downloads (regenerate with: helm dependency build)
gkg/charts/*.tgz
gkg/Chart.lock
observability/charts/*.tgz
observability/Chart.lock
observability/tmpcharts-*

# Local secrets (contains passwords)
.tilt-secrets
